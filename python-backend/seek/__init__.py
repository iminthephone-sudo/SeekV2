"""SEEK engine pack — job-readiness workflows for justice outreach programs.

Python is engine-only. The Qt/C++ shell owns every window; it talks to these
engines exclusively through ``seek_cpp_bridge.py`` (see docs/BRIDGE_CONTRACT.md).
"""

__version__ = "2.0.0"
APP_NAME = "SEEK"
